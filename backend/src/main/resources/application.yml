spring:
  application:
    name: aiprojecteval
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  config:
    import: optional:file:.env[.properties]

server:
  port: ${SERVER_PORT:8080}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}
  jwt:
    secret: ${JWT_SECRET:replace-this-with-a-long-development-secret-key-at-least-32-characters}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}
  ai:
    provider: ${APP_AI_PROVIDER:MOCK}
    model: ${APP_AI_MODEL:mock-project-proposal-evaluator-v1}
    api-key: ${APP_AI_API_KEY:}
    temperature: ${APP_AI_TEMPERATURE:0.2}
    max-output-tokens: ${APP_AI_MAX_OUTPUT_TOKENS:2048}
    timeout-seconds: ${APP_AI_TIMEOUT_SECONDS:30}

document:
  google-drive:
    validation-enabled: ${DOCUMENT_GOOGLE_DRIVE_VALIDATION_ENABLED:false}
    max-file-size-mb: ${DOCUMENT_GOOGLE_DRIVE_MAX_FILE_SIZE_MB:10}
    service-account-json-path: ${DOCUMENT_GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON_PATH:${GOOGLE_APPLICATION_CREDENTIALS:}}
    allowed-mime-types:
      - application/pdf
      - application/vnd.openxmlformats-officedocument.wordprocessingml.document
      - application/vnd.google-apps.document
